<UserControl x:Class="PicoTorrentBootstrapper.Views.InstallView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:PicoTorrentBootstrapper.Converters"
             xmlns:local="clr-namespace:PicoTorrentBootstrapper.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" TrueValue="Visible" FalseValue="Hidden" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Content="{Binding InstallView}" />

        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Grid.Row="1" Margin="10,5,10,10">
            <Button Padding="3" Margin="0,0,5,0">Cancel</Button>
            <Button Padding="3" Command="{Binding InstallCommand}" IsEnabled="{Binding InstallEnabled}">Install PicoTorrent</Button>
        </StackPanel>
    </Grid>
</UserControl>
