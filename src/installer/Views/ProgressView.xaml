<UserControl x:Class="PicoTorrentBootstrapper.Views.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="10,5" Orientation="Horizontal" Grid.Row="1">
            <TextBlock FontWeight="Bold" Margin="0,0,5,0">Status</TextBlock>
            <TextBlock Text="{Binding Message}" TextTrimming="CharacterEllipsis"></TextBlock>
        </StackPanel>
        <ProgressBar Grid.Row="2" Margin="10,0,10,10" Height="20" Value="{Binding Progress}"></ProgressBar>
    </Grid>
</UserControl>
