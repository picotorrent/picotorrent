#include "addtorrentdlg.hpp"

using pt::AddTorrentDialog;

AddTorrentDialog::AddTorrentDialog(wxWindow* parent)
	: wxDialog(parent, wxID_ANY, "Add torrent(s)", wxDefaultPosition, wxSize(400, 300))
{
}
